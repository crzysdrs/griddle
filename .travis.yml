language: haskell
before_install:
  - cabal install codecov-haskell
script:
  - cabal configure --enable-tests --enable-library-coverage && cabal build
  - run-cabal-test test
after_script:
  - codecov-haskell --display-report --print-response --exclude-dir=test test